import { Routes } from '@angular/router'
import { {{className}}Component } from './{{name}}-page.component'

export const {{name}}Routes: Routes = [
  { path: '', component: {{className}}Component }
]
